[Service]
# Disable zswap before zram swap starts to avoid layering the two compressors.
ExecStartPre=/bin/sh -c 'if [ -e /sys/module/zswap/parameters/enabled ]; then echo N > /sys/module/zswap/parameters/enabled; fi'
